[project]
name = "template-next-app"
version = "0.1.0"
description = "Codified architecture diagram for the template-next-app project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cairosvg>=2.9.0",
    "diagrams>=0.25.0",
    "pillow>=12.3.0",
]
